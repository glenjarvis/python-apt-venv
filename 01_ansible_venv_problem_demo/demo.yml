---
- name: Demo Playbook
  hosts: localhost
  tasks:
    - debug: msg="Ansible is working."

- name: Demo Playbook
  hosts: localhost
  tasks:
    - apt:
        name: cowsay
        state: present

